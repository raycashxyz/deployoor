node_modules
dist
*.tsbuildinfo
.turbo
coverage
*.log
.DS_Store
.env
.env.*
!.env.example

# Committed brand/demo assets live under assets/brand/dist even though the blanket
# `dist` rule above would otherwise ignore them (they are referenced by README.md).
!assets/brand/dist/
!assets/brand/dist/**

# VHS's full-res output is a regenerable intermediate (~2.5MB). Only the optimized
# demo-sm.gif that README.md embeds is committed — `pnpm demo:record` rebuilds both.
assets/brand/dist/demo.gif
