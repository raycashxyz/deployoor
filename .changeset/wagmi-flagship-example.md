---
"@example/hardhat": patch
---

Complete the end-to-end journey in one folder: add `wagmi.config.ts` and a committed `src/generated.ts` so the example now shows deploy → committed JSON record → typed app access, with both ends readable without running anything. `pnpm --filter @example/hardhat e2e` also runs `wagmi generate`.
