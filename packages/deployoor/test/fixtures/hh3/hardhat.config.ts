// Minimal Hardhat 3 config shape (fixture only — not compiled here). Presence of
// hardhat.config.ts makes detectFramework report "hardhat".
export default {
  plugins: [],
  solidity: { profiles: { default: { version: "0.8.35" } } },
};
