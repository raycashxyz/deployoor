{
  "name": "@example/custom-paths",
  "version": "0.0.1",
  "private": true,
  "description": "deployoor on a project where every path is moved: Hardhat sources, artifacts and cache, plus deployoor's own out and deploymentsPath. Shows which paths deployoor reads from your framework's config and which need a deployoor config.",
  "scripts": {
    "generate": "hardhat compile",
    "test:memory": "hardhat compile && vitest run",
    "e2e": "hardhat compile && vitest run"
  },
  "dependencies": {
    "deployoor": "workspace:*",
    "viem": "catalog:"
  },
  "devDependencies": {
    "@deployoor/hardhat": "workspace:*",
    "@deployoor/testing": "workspace:*",
    "@types/node": "catalog:",
    "hardhat": "^2.22.0",
    "tsx": "^4.23.12",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
