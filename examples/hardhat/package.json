{
  "name": "@example/hardhat",
  "version": "0.0.0",
  "private": true,
  "description": "deployoor end-to-end on a Hardhat project: compile → auto-generate typed deployers → deploy (recorded to deployments/) → consume with wagmi. Also tests on an in-memory EVM.",
  "scripts": {
    "generate": "hardhat compile",
    "deploy": "tsx --env-file-if-exists=.env scripts/deploy.ts",
    "wagmi:generate": "wagmi generate",
    "e2e": "hardhat compile && vitest run"
  },
  "dependencies": {
    "deployoor": "workspace:*",
    "viem": "catalog:"
  },
  "devDependencies": {
    "@deployoor/hardhat": "workspace:*",
    "@deployoor/testing": "workspace:*",
    "@deployoor/wagmi": "workspace:*",
    "@types/node": "catalog:",
    "@wagmi/cli": "catalog:",
    "hardhat": "^2.22.0",
    "tsx": "^4.22.4",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
