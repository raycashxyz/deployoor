{
  "name": "@example/hardhat",
  "version": "0.0.4",
  "private": true,
  "description": "deployoor on a Hardhat project: compile → auto-generate typed deployers → deploy, recorded to deployments/, then consumed as typed contracts via @wagmi/cli. Also tests on an in-memory EVM.",
  "scripts": {
    "generate": "hardhat compile",
    "deploy": "tsx --env-file-if-exists=.env scripts/deploy.ts",
    "wagmi": "wagmi generate",
    "e2e": "hardhat compile && vitest run && wagmi generate"
  },
  "dependencies": {
    "deployoor": "workspace:*",
    "viem": "catalog:"
  },
  "devDependencies": {
    "@deployoor/hardhat": "workspace:*",
    "@deployoor/testing": "workspace:*",
    "@deployoor/wagmi": "workspace:*",
    "@types/node": "catalog:",
    "@wagmi/cli": "catalog:",
    "@wagmi/core": "^2.21.1",
    "hardhat": "^2.22.0",
    "tsx": "^4.22.4",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
