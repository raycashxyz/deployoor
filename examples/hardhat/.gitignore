node_modules
artifacts
cache
.env
# NOTE: deployers/ and deployments/ are both intentionally committed. The records are the source of
# truth, and the deployers are what make a fresh clone typecheck and deploy without running
# `deployoor generate` first.
