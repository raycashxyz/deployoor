# Copy to .env and fill in. `pnpm deploy` reads these.
# Defaults below are a local anvil node (`anvil`) + its first prefunded dev account.
# For a testnet, point RPC_URL at your provider and PRIVATE_KEY at a funded key.
RPC_URL=http://127.0.0.1:8545
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
