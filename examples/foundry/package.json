{
  "name": "@example/foundry",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "description": "deployoor with a Foundry project: forge build → deployoor generate → deploy & test on an in-memory EVM.",
  "scripts": {
    "generate": "pnpm --filter deployoor build && forge build && pnpm exec deployoor generate",
    "e2e": "pnpm --filter deployoor build && forge build && pnpm exec deployoor generate && vitest run"
  },
  "dependencies": {
    "deployoor": "workspace:*",
    "viem": "catalog:"
  },
  "devDependencies": {
    "@deployoor/testing": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
